# Core scientific stack
numpy==2.2.2
pandas==2.2.3
matplotlib==3.10.0
seaborn==0.13.2

# ML
scikit-learn==1.5.2       # >=1.5 has solid NumPy 2 support
xgboost>=2.1.0,<3.0.0     # 2.1+ supports NumPy 2.x

# Spark
pyspark==3.5.5
# PyArrow is used by PySpark for Arrow UDFs / vectorized toPandas
# Option A (safest): let pip resolve a compatible version
# pyarrow
# Option B (explicit range): widen to avoid breakage if 18.x is too new for your Spark build
pyarrow>=14.0.0,<19.0.0

# Utilities
holidays==0.58
jupyterlab==4.3.5
herbie-data
cfgrib
xarray
boto3